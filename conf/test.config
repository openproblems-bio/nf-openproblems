/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 *   nextflow run nf-core/openproblems -profile test
 */

params {
  config_profile_name = 'Test profile'
  config_profile_description = 'Minimal test dataset to check pipeline function'

  max_memory = 16.GB
  max_cpus = 4
  max_time = 60.min

  withName: 'load_dataset' {
    cpus = 4
    memory = { check_max( 8.GB * (2 ** task.attempt), 'memory' ) }
    time = 1.h
  }

  withName: 'run_method' {
    cpus = { (task.attempt == 1) ? 2 : 4 }
    memory = { check_max( 4.GB * (2 ** task.attempt), 'memory' ) }
    time = { (task.attempt == 1) ? 10.min : 60.min }
  }

  withName: 'run_metric' {
    cpus = { (task.attempt == 1) ? 2 : 4 }
    memory = { check_max( 4.GB * (2 ** task.attempt), 'memory' ) }
    time = { (task.attempt == 1) ? 10.min : 60.min }
  }

  use_test_data = true
}
